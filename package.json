{
  "name": "prompt-eval-workbench",
  "private": true,
  "description": "Streamlit chat + Langfuse; Promptfoo eval via promptfooconfig.yaml",
  "scripts": {
    "test:promptfoo": "promptfoo eval --env-path .env --no-write --no-cache -o promptfoo-results.json",
    "sync:langfuse": "python3 sync_promptfoo_to_langfuse.py",
    "test:promptfoo:langfuse": "npm run test:promptfoo && npm run sync:langfuse",
    "verify:promptfoo": "promptfoo --version"
  },
  "devDependencies": {
    "promptfoo": "0.103.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
