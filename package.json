{
  "name": "prompt-eval-workbench",
  "private": true,
  "description": "LLM eval workbench: Streamlit, Langfuse, Promptfoo (portfolio)",
  "scripts": {
    "test:promptfoo": "promptfoo eval --config config/promptfooconfig.yaml --env-path .env --no-write --no-cache -o promptfoo-results.json",
    "sync:langfuse": "python3 -m prompt_eval_workbench.promptfoo_sync",
    "test:promptfoo:langfuse": "npm run test:promptfoo && npm run sync:langfuse",
    "verify:promptfoo": "promptfoo --version"
  },
  "devDependencies": {
    "promptfoo": "0.103.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
